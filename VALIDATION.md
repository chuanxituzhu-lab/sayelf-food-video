# sayelf · v1.7.4 验证记录

日期：2026-09-06。测试对象为 `sayelf-food-video.html` 的 shipping 文件和独立 `SKILL.md`。测试不调用外部模型、不上传素材；GitHub 上传仅包含已审查的 Public 商品文件。

## 已通过

- 核心抽取后独立运行：25 项测试通过。
- 11 个一键示范均可生成图片 Prompt、视频 Prompt 和连续分镜。
- 六道川菜均有四段工艺 `选材 → 处理 → 烹制 → 盛盘`，并输出专属器皿、场景路线和声音设计。
- 场景模式验证通过：自动模式读取菜品路线，指定场景可覆盖环境路线并保留器物/食材连续性。
- 画面比例验证通过：自动匹配、9:16、3:4、16:9、4:5 均能写入图片 Prompt、视频 Prompt 和输出元数据。
- 真人秀标题与人设会按菜品自动切换；品牌、创意主题、场景补充和本机参照照片文件名会进入双 Prompt，照片二进制不写入配置。
- 时长 6–60 秒（每 0.1 秒，共 5,951 个“时长点 × 示范”组合）均通过连续性检查：起点 0、无空隙/重叠、末点等于输入时长。
- 每次输出均通过 `imageFrames.length === shots.length`，且图片帧与视频分镜逐一共享 `id/start/end`。
- 自定义菜名可从普通输入或专业设置进入；未收录菜品使用川味通用四段工艺、器皿和场景路线，并在警告中标注为通用建议。
- 证据门控：无事件/分歧证据时不写入冲突事实；完整十节点仅在真人秀、时长和证据条件满足时按顺序使用。
- ASMR、BGM、字幕、水印、参考说明、对白开关均有覆盖；无 BGM 时保留现场声。
- 配置 JSON 导入/版本拒绝/下载回环通过；导入内容按数据读取，不执行脚本。
- shipping HTML 在 Microsoft Edge headless 中通过 18 项界面检查：`file:` 打开、11 个示范点击生成、普通用户仅用菜品/场景生成、普通输入自定义菜名、菜品切换真人秀标题、专业设置与专业输出默认折叠、专业用户自定义菜名与画面比例、本机参照照片预览且不上传、分镜 Prompt 默认折叠并可单独打开复制、断网生成、编辑后导出禁用、错误输入阻断、HTML-like 输入保持文本、标签切换、TXT/JSON 下载、合法/非法 JSON 导入、复制反馈、移动端无横向溢出。
- 浏览器运行时错误：0；外部页面请求：0。
- 人工视觉检查：桌面和 390px 移动布局可读；深色工作台、顶部状态栏、输入/结果双栏、横向分镜卡片、标签页和高级折叠层次明确；普通用户自定义菜名输入框为绿色底、输入文字和占位文字为白色；示范分镜的角度、移动方式和方向无完全重复，分镜 Prompt 显示动作/视线/器物/声音桥转场，移动端仍无横向溢出。
- ZIP 包内容与输出目录一致，未包含课堂截图、视频、个人临时路径、密钥或第三方代码；待上传文件清单只包含商品文档、独立 HTML 和 ZIP。

## 证据文件（开发留存）

- `work/core-test-results.json`
- `work/browser-test-results.json`
- `work/desktop-preview.png`
- `work/mobile-preview.png`

这些留存文件不放入商品 ZIP，避免把本机测试上下文带入交付物。

## 限制与未测行为

- `quick_validate.py` 未能运行，因为当前 Python 环境缺少其 `yaml` 依赖；已人工检查 frontmatter 仅含 `name`、`description`，命名为小写连字符，且没有脚手架占位符。该环境问题不影响浏览器/核心测试，但若发布 Skill，建议在带 PyYAML 的环境再跑一次官方 quick validator。
- 未调用真实图片/视频模型，因此未验证供应商对参考图、无水印、最长时长、声音同步或生成质量的支持。Prompt 只是一份计划。
- HTML 不核实事件是否真实，不判断肖像/声音授权，也不预测传播效果。`evidenceConfirmed` 是用户声明，不是代理核验。
- 系统剪贴板是否同步由操作系统决定；本页不主动上传。平台建议是编辑默认值，不是官方算法结论。
- GitHub 上传不包含课堂截图、原会话、测试截图、测试结果、个人路径或凭据；运行时用户输入不随仓库发布。

## 回滚

替换为上一版 HTML、Skill 和配置即可回滚。v1.7.4 输出 schema 仍为 `sayelf-food-video/v1.5`（本次增加自然转场编译与验证，未改变配置结构）；配置输入 schema 仍为 `sayelf-food-video/v1`，并兼容读取 v1/v1.1/v1.2/v1.3/v1.4/v1.5。未知输入 schema 会拒绝，不静默迁移。
